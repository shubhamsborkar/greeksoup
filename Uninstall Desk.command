#!/bin/zsh
# Double-click me to remove GreekSoup from this Mac. It stops the desk, removes the
# start-at-login entry, saves a copy of your lists to the Desktop, and asks before
# deleting the folder.
cd "$(dirname "$0")"
bash ./uninstall.sh
echo
echo "  You can close this window."
